import { PathApiBackPipe } from './path-api-back.pipe';

describe('PathApiBackPipe', () => {
  it('create an instance', () => {
    const pipe = new PathApiBackPipe();
    expect(pipe).toBeTruthy();
  });
});
